'use client';
import { useState } from 'react';import Link from 'next/link';import { supabase, supabaseReady } from '@/lib/supabaseClient';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');async function submit(){if(!supabaseReady){setMsg('Configure Supabase dans Vercel avant la connexion réelle.');return}const {error}=await supabase.auth.signInWithPassword({email,password});setMsg(error?error.message:'Connexion réussie. Va au tableau de bord.')}return <div style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'linear-gradient(135deg,#0046ad,#0072ce)'}}><div className="card" style={{width:'min(430px,92vw)',padding:30}}><h1 style={{textAlign:'center'}}>Consignes Permanentes</h1><p style={{textAlign:'center',color:'#0046ad',fontWeight:800}}>Castorama Claye-Souilly</p><label>Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="prenom.nom@exemple.fr"/><br/><br/><label>Mot de passe</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn btn-primary" style={{width:'100%',marginTop:18}} onClick={submit}>Se connecter</button>{msg&&<p style={{color:'#475569'}}>{msg}</p>}<p style={{fontSize:13,textAlign:'center'}}>Pas encore de compte ? <Link href="/register">Demander un accès</Link></p></div></div>}

'use client';
import { useState } from 'react';import AppShell from '@/components/AppShell';
export default function AdminConsignes(){const [title,setTitle]=useState('');const [cat,setCat]=useState('Sécurité');const [prio,setPrio]=useState('Normale');const [content,setContent]=useState('');return <AppShell><h1>Administration des consignes</h1><div className="card" style={{padding:24,maxWidth:850}}><label>Titre</label><input className="input" value={title} onChange={e=>setTitle(e.target.value)}/><br/><br/><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12}}><div><label>Catégorie</label><select className="select" value={cat} onChange={e=>setCat(e.target.value)}>{['Sécurité','Maintenance','Logistique','Jardin','Bâti','Caisse','Direction'].map(c=><option key={c}>{c}</option>)}</select></div><div><label>Priorité</label><select className="select" value={prio} onChange={e=>setPrio(e.target.value)}>{['Basse','Normale','Haute','Urgente'].map(c=><option key={c}>{c}</option>)}</select></div></div><br/><label>Contenu</label><textarea className="textarea" rows={8} value={content} onChange={e=>setContent(e.target.value)}></textarea><button className="btn btn-primary" style={{marginTop:16}}>Enregistrer</button><p style={{color:'#64748b'}}>La sauvegarde Supabase sera activée après configuration des clés.</p></div></AppShell>}

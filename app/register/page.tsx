'use client';
import { useState } from 'react';import Link from 'next/link';import { supabase, supabaseReady } from '@/lib/supabaseClient';
export default function Register(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [msg,setMsg]=useState('');async function submit(){if(!supabaseReady){setMsg('Configure Supabase pour activer les inscriptions.');return}const {error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name,status:'pending',role:'user'}}});setMsg(error?error.message:'Demande envoyée. Un administrateur devra valider ton compte.')}return <div style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#f6f8fc'}}><div className="card" style={{width:'min(470px,92vw)',padding:30}}><h1>Demande d’accès</h1><label>Nom complet</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/><br/><br/><label>Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)}/><br/><br/><label>Mot de passe</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn btn-primary" style={{width:'100%',marginTop:18}} onClick={submit}>Envoyer la demande</button>{msg&&<p>{msg}</p>}<Link href="/login">Retour connexion</Link></div></div>}
